export type ArcanaMapItem = {
    id: number;
    arcana_name: string;
    icon: string;
}

export type ElemMapItem = {
    id: number;
    elem_name: string;
    color: string;
}

export type ElemMapItem = {
    id: number;
    elem_name: string;
    color: string;
}

export type ElemMapItem = {
    id: number;
    elem_name: string;
    color: string;
}